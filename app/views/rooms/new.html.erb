<h2 class="text-xl font-bold mb-4">Nouvelle chambre pour l'hôtel <%= @hotel.name %></h2>
<%= render 'form', room: @room %>
<%= link_to "⬅ Retour à l'hôtel", hotel_path(@hotel), class: "text-blue-600 underline mt-4 block" %>